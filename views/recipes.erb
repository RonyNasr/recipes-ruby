<h1>Recipes</h1>

<% if @recipes.any?() %>
  <% @recipes.each() do |recipe| %>
    <h5><a href="/recipes/view/<%= recipe.id() %>/ingredients"><%= recipe.name() %></a></h5>
  <% end %>
<% else %>
  There are no recipes.
<% end %>

<a class="btn btn-default" href="/">Back</a>
